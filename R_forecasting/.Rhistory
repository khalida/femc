historicData = vector(length=100)
historicDataTimeSeries = ts(historicData)
forecastEts <- forecast(historicDataTimeSeries, h=horizon$V1,
find.frequency=TRUE, level=FALSE,
robust=TRUE)
library(forecast)
forecastEts <- forecast(historicDataTimeSeries, h=horizon$V1,
find.frequency=TRUE, level=FALSE,
robust=TRUE)
plot(historicDataTimeSeries)
var(historicDataTimeSeries)
var(historicDataTimeSeries) == 0
var(historicDataTimeSeries) != 0
mean(historicDataTimeSeries)
rep(mean(historicDataTimeSeries),times=horizon$V1)
rep(mean(historicDataTimeSeries),times=10)
horizon$V1 <- 10
horizon <- NULL; horizon$V1 <- 10;
if(var(historicDataTimeSeries) == 0) {
forecastEts <- NULL;
forecastEts$mean <- rep(mean(historicDataTimeSeries),times=horizon$V1)
} else {
# Carry out automated ETS forecast
forecastEts <- forecast(historicDataTimeSeries, h=horizon$V1,
find.frequency=TRUE, level=FALSE,
robust=TRUE)
}
